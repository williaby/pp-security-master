name: CI Gate
on:
  pull_request:
  push:
    branches: [main]
permissions:
  contents: read
jobs:
  ci-gate:
    name: CI Gate
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "stub"
